// Compatibility.swift
// Previously provided a typealias from `FirestoreService` to `FirestoreManager`.
// Removed to avoid invalid redeclaration when a `FirestoreService` symbol already exists in the project.

import Foundation

// Intentionally left blank to avoid duplicate symbol definitions.
