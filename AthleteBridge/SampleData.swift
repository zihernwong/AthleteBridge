//
//  SampleData.swift
//  Replaced by Firestore-backed data. Keep these stubs for compatibility with older views.
//

let coaches: [Coach] = [] // Use FirestoreService.shared or inject FirestoreService into views instead

let sampleClient: Client? = nil // Fetch real client via FirestoreService.fetchClient

// NOTE: Remove or migrate usages of `coaches` and `sampleClient` to use FirestoreService.
